import { Typography } from "@mui/material";
import React from "react";

const Checkout = () => {
  return (
    <Typography variant="h3">
      Only logged in user should be able to see this!
    </Typography>
  );
};

export default Checkout;
